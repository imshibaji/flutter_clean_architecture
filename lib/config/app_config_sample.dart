class Config {
  // Rapid Host Endpoint
  static String host = 'free-news.p.rapidapi.com';
  static String key = '<use your own auth key>';

  // Server Secrate Token
  static String token = 'Bearer <use your own auth token>';
}
